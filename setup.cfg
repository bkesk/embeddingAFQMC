[metadata]
name = embedding
version = 0.0.2

[options]
install_requires =
    numpy
    h5py
    pyscf
    pyyaml
    matplotlib
packages=find:

[options.entry_points]
console_scripts =
    embed = embedding.__main__:main
    test_em = embedding.test
